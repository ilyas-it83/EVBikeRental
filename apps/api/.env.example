# API Environment Variables

# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Database (SQLite — file path relative to project root)
DATABASE_URL=./data/ev-bike-rental.db
